import React from 'react';
import './Button.scss';

const Button = () => {
    return (
        <button className="Button">
            <span>START </span>
        </button>
    )
}

export default Button;